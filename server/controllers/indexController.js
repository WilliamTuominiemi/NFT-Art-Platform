const Drawing = require('../models/image')

const main = (req, res) => {
    console.log('request')
    // Drawing.find()
    //     .sort({ likes: -1 })
    //     .then((result) => {
    //         console.log(result)
    //     })
}

module.exports = {
    main,
}
